# ROADMAP — Heroes of Rust

Детальный план разработки по этапам. Каждый этап разбит на конкретные задачи.
Статусы: ✅ готово | 🔄 в работе | ⬜ не начато

> **Живой документ.** Задачи начиная с Этапа 2 носят ориентировочный характер и будут уточняться по мере прохождения предыдущих этапов. Не нужно планировать далеко вперёд — детализировать следующий этап стоит только после завершения текущего.

---

## Этап 0 — Подготовка репозитория

**Цель:** создать основу проекта, убедиться что `cargo run` открывает окно.

### 0.1 Инициализация проекта

- [ ] Выполнить `git init`
- [ ] Выполнить `cargo new heroes_of_rust --bin`
- [ ] Проверить актуальную версию Rust (`rustup show`) и зафиксировать в `rust-toolchain.toml`
- [ ] Добавить `.gitignore` (исключить `target/`, `.env`)
- [ ] Добавить `README.md` с кратким описанием проекта и командами разработки
- [ ] Сделать первый коммит: `git commit -m "chore: init project"`

### 0.2 Зависимости

- [ ] Добавить `bevy` в `Cargo.toml` (последняя стабильная версия)
- [ ] Добавить фичу `bevy/dynamic_linking` для ускорения сборки в dev-режиме
- [ ] Добавить `[profile.dev]` оптимизации в `Cargo.toml`:
  - `opt-level = 1` для самого крейта
  - `opt-level = 3` для зависимостей (`[profile.dev.package."*"]`)

### 0.3 Инструменты качества кода

- [ ] Добавить `rustfmt.toml` с базовыми настройками форматирования
- [ ] Добавить `.clippy.toml` или `#![deny(clippy::...)]` в `main.rs`
- [ ] Убедиться что `cargo fmt` и `cargo clippy` работают без ошибок

### 0.4 Минимальное окно Bevy

- [ ] Написать `main.rs` с `App::new()` и `DefaultPlugins`
- [ ] Задать заголовок окна: `"Heroes of Rust"`
- [ ] Задать начальный размер окна (например, 1280×720)
- [ ] Добавить однотонный фоновый цвет (`ClearColor`)
- [ ] Убедиться что `cargo run` открывает окно без паники и ошибок

**Критерий готовности:** `cargo run` стабильно открывает пустое окно игры.

---

## Этап 1 — Чистая игровая модель

**Цель:** реализовать ядро игровой логики без какой-либо графики. Всё покрыто тестами.

### 1.1 Структура модулей

- [ ] Создать `src/core/mod.rs`
- [ ] Создать подмодули: `map`, `hero`, `player`, `resources`, `town`, `commands`
- [ ] Подключить `mod core;` в `main.rs`

### 1.2 Карта и позиции (`core::map`)

- [ ] Объявить `Position { x: i32, y: i32 }` с `PartialEq`, `Eq`, `Hash`, `Clone`, `Copy`, `Debug`
- [ ] Объявить `TileKind`: `Ground`, `Obstacle`, `Water`
- [ ] Объявить `Tile { kind: TileKind, object: Option<MapObject> }`
- [ ] Объявить `MapObject`: `ResourcePile(ResourceBag)`, `Town(TownId)`, `NeutralArmy(Army)`
- [ ] Объявить `AdventureMap { width, height, tiles: Vec<Tile> }`
- [ ] Реализовать `AdventureMap::new(width, height)` — заполнить `Ground`-тайлами
- [ ] Реализовать `AdventureMap::get(pos)` / `get_mut(pos)` — возврат `Option<&Tile>`
- [ ] Реализовать `AdventureMap::is_passable(pos)` — проверка проходимости
- [ ] Реализовать `AdventureMap::neighbors(pos)` — 4 соседних клетки (в пределах карты)

### 1.3 Ресурсы (`core::resources`)

- [ ] Объявить `ResourceKind`: `Gold` (только этот на старте)
- [ ] Объявить `ResourceBag { gold: u32 }`
- [ ] Реализовать `ResourceBag::add(other: &ResourceBag)`
- [ ] Реализовать `ResourceBag::subtract(other: &ResourceBag) -> Result<(), InsufficientFunds>`
- [ ] Реализовать `ResourceBag::can_afford(cost: &ResourceBag) -> bool`

### 1.4 Существа и армия (`core::hero`)

- [ ] Объявить `UnitType { name, damage_per_unit: u32, hp: u32, cost: ResourceBag }`
- [ ] Объявить `UnitStack { unit_type: UnitType, count: u32, hp_remaining: u32 }`
- [ ] Реализовать `UnitStack::is_alive() -> bool`
- [ ] Объявить `Army(Vec<UnitStack>)` (до 7 слотов — как в оригинале)
- [ ] Реализовать `Army::add_stack(stack: UnitStack)`
- [ ] Реализовать `Army::is_empty() -> bool`

### 1.5 Герой (`core::hero`)

- [ ] Объявить `HeroId(u32)`
- [ ] Объявить `Hero { id: HeroId, name: String, position: Position, army: Army, movement_points: u32, movement_points_max: u32 }`
- [ ] Реализовать `Hero::can_move_to(pos, map) -> bool`
- [ ] Реализовать `Hero::spend_movement(cost: u32) -> Result<(), NoMovement>`
- [ ] Реализовать `Hero::restore_movement()`

### 1.6 Игрок и состояние игры (`core::player`, `core::state`)

- [ ] Объявить `PlayerId(u32)`
- [ ] Объявить `Player { id: PlayerId, resources: ResourceBag, hero_ids: Vec<HeroId> }`
- [ ] Объявить `TownId(u32)`
- [ ] Объявить `Town { id: TownId, position: Position, garrison: Army, available_recruits: Vec<(UnitType, u32)> }`
- [ ] Объявить `GameState { map, players, heroes, towns, current_day, active_player_id }`

### 1.7 Команды игрока (`core::commands`)

- [ ] Объявить `GameCommand` enum:
  - `MoveHero { hero_id, target: Position }`
  - `CollectResource { hero_id }`
  - `RecruitUnits { town_id, unit_type_idx: usize, count: u32 }`
  - `EndTurn`
- [ ] Объявить `CommandError` enum с вариантами для всех ошибок
- [ ] Реализовать `GameState::apply(command) -> Result<Vec<GameEvent>, CommandError>`
- [ ] Реализовать логику `MoveHero`:
  - проверить что герой принадлежит активному игроку
  - проверить что клетка проходима и смежна с текущей позицией
  - проверить наличие очков движения
  - переместить героя, списать очки движения
  - если на клетке объект — вернуть событие взаимодействия
- [ ] Реализовать логику `CollectResource`:
  - если на клетке героя есть `ResourcePile` — добавить ресурс игроку, убрать объект с карты
- [ ] Реализовать логику `RecruitUnits`:
  - проверить что герой находится в городе
  - проверить что существ достаточно в городе
  - списать золото, добавить стек в армию героя
- [ ] Реализовать логику `EndTurn`:
  - восстановить очки движения всех героев
  - начислить доход города
  - увеличить счётчик дня

### 1.8 События (`core::events`)

- [ ] Объявить `GameEvent` enum:
  - `HeroMoved { hero_id, from: Position, to: Position }`
  - `ResourceCollected { hero_id, amount: ResourceBag }`
  - `BattleStarted { attacker: HeroId, defender_pos: Position }`
  - `TurnEnded { day: u32 }`
  - `DayIncome { player_id, amount: ResourceBag }`

### 1.9 Unit-тесты (`core`)

- [ ] Тест: герой движется на свободную клетку — успех
- [ ] Тест: герой движется на препятствие — ошибка
- [ ] Тест: герой движется без очков движения — ошибка
- [ ] Тест: герой собирает ресурс с клетки — золото зачислено, объект удалён
- [ ] Тест: герой пытается собрать с пустой клетки — ошибка
- [ ] Тест: найм существ за достаточное золото — успех
- [ ] Тест: найм существ при нехватке золота — ошибка
- [ ] Тест: завершение хода восстанавливает очки движения
- [ ] Тест: завершение хода начисляет доход города

**Критерий готовности:** `cargo test` проходит все тесты без запуска окна.

---

## Этап 2 — Карта приключений на экране

**Цель:** увидеть карту и управлять героем мышью или клавиатурой.

### 2.1 Модуль adventure

- [ ] Создать `src/adventure/mod.rs`, `render.rs`, `input.rs`, `sync.rs`
- [ ] Зарегистрировать `AdventurePlugin` в `main.rs`

### 2.2 Ресурс GameState в Bevy

- [ ] Обернуть `GameState` в Bevy-ресурс (`Resource`)
- [ ] Инициализировать тестовую карту 16×12 в `startup`-системе:
  - несколько клеток `Obstacle`
  - герой в стартовой позиции

### 2.3 Отрисовка карты

- [ ] Выбрать размер тайла (например, 48×48 пикселей)
- [ ] В startup-системе заспавнить `SpriteBundle` для каждого тайла
  - `Ground` — зелёный цвет или временный спрайт
  - `Obstacle` — серый цвет
  - `Water` — синий цвет
- [ ] Настроить камеру (`Camera2dBundle`) по центру карты

### 2.4 Отрисовка героя

- [ ] Заспавнить сущность героя с компонентом `HeroMarker(HeroId)` и спрайтом
- [ ] Написать систему `sync_hero_transform` — синхронизировать `Transform` героя с `hero.position` из `GameState`

### 2.5 Отображение очков движения

- [ ] Добавить текстовый UI-элемент `"MP: X / Y"` поверх сцены
- [ ] Писать систему `update_movement_ui` — обновлять текст при изменении `Hero::movement_points`

### 2.6 Управление: клавиатура

- [ ] Система `keyboard_input`:
  - `WASD` или стрелки → вычислить целевую позицию
  - Отправить `GameCommand::MoveHero` в `GameState::apply`
  - Обработать `CommandError` (залогировать или показать в UI)

### 2.7 Управление: мышь

- [ ] Система `mouse_click_input`:
  - Перевести экранные координаты клика в `Position` на сетке
  - Отправить `GameCommand::MoveHero`
- [ ] Визуально выделить клетку под курсором (hover highlight)

### 2.8 Отображение доступных ходов

- [ ] Вычислить список проходимых соседних клеток от позиции героя
- [ ] Визуально подсвечивать их (полупрозрачный спрайт поверх тайла)
- [ ] Обновлять при каждом ходе

**Критерий готовности:** игрок двигает героя по карте мышью или клавиатурой, видит очки движения.

---

## Этап 3 — Ресурсы и экономика

**Цель:** первая полезная интеракция — собрать золото.

### 3.1 Объекты на карте

- [ ] В `AdventureMap` поддержать `MapObject::ResourcePile`
- [ ] При инициализации карты разместить 3–5 кучек золота в случайных/фиксированных позициях

### 3.2 Отрисовка ресурсов

- [ ] Заспавнить спрайт/маркер для каждой `ResourcePile` на карте
- [ ] Привязать компонент `MapObjectMarker(Position)` к сущности
- [ ] Убирать спрайт при сборе ресурса (реагируя на `GameEvent::ResourceCollected`)

### 3.3 Автосбор при входе на клетку

- [ ] После успешного `MoveHero` проверять событие взаимодействия
- [ ] Если герой вошёл на клетку с `ResourcePile` — автоматически применять `CollectResource`

### 3.4 UI ресурсов

- [ ] Добавить панель ресурсов в верхней части экрана: `"Золото: X"`
- [ ] Система `update_resource_ui` — обновлять при получении `DayIncome` или `ResourceCollected`

### 3.5 Завершение хода и доход

- [ ] Добавить кнопку «Завершить ход» (Bevy UI или просто клавиша `Enter`/`Space`)
- [ ] При нажатии применять `GameCommand::EndTurn`
- [ ] Отображать текущий день: `"День X"`

**Критерий готовности:** игрок собирает золото, видит изменение баланса, завершает ход.

---

## Этап 4 — Город и найм существ

**Цель:** тратить золото, увеличивать армию.

### 4.1 Город на карте

- [ ] Добавить `MapObject::Town(TownId)` в тайл карты
- [ ] Разместить один город на стартовой позиции
- [ ] Отрисовать спрайт/маркер города

### 4.2 Вход в город

- [ ] При движении героя на клетку с городом — открывать экран города
- [ ] Вводить состояние `GameScreen::Town` (через Bevy States или флаг-ресурс)

### 4.3 Экран города

- [ ] Отрисовать простое окно поверх карты (Bevy UI)
- [ ] Показать список доступных для найма существ:
  - название, стоимость, количество доступных, количество в армии героя
- [ ] Кнопки `+` / `-` для выбора количества для найма
- [ ] Кнопка `Нанять` — отправить `GameCommand::RecruitUnits`
- [ ] Кнопка `Закрыть` — вернуться на карту

### 4.4 Отображение армии

- [ ] На экране города показывать текущую армию героя (список стеков)
- [ ] На панели карты — краткое отображение армии (иконки или текст)

### 4.5 Прирост существ

- [ ] При `EndTurn` пополнять количество доступных существ в городе на фиксированное значение
- [ ] Логика в `GameState::apply(EndTurn)`

**Критерий готовности:** игрок заходит в город, нанимает существ за золото, видит изменение армии.

---

## Этап 5 — Простой бой

**Цель:** добавить конфликт. Герой атакует нейтральный отряд и побеждает.

### 5.1 Нейтральный отряд на карте

- [ ] Добавить `MapObject::NeutralArmy(Army)` в карту
- [ ] Разместить 1–2 нейтральных отряда на карте
- [ ] Отрисовать маркер нейтрального отряда

### 5.2 Переход в режим боя

- [ ] При движении героя на клетку с `NeutralArmy` — генерировать `GameEvent::BattleStarted`
- [ ] Переключить состояние на `GameScreen::Battle`
- [ ] Передать в `BattleState`:
  - атакующая армия (Hero.army)
  - защищающаяся армия (NeutralArmy)

### 5.3 Модуль боя (`src/battle/`)

- [ ] Создать `src/battle/mod.rs`, `state.rs`, `render.rs`, `input.rs`
- [ ] Объявить `BattleState`:
  - `attacker_stacks: Vec<BattleStack>`
  - `defender_stacks: Vec<BattleStack>`
  - `turn_order: VecDeque<StackId>` (пока просто чередовать)
  - `current_stack: StackId`
- [ ] Объявить `BattleStack { id: StackId, unit_type, count, hp_remaining, side: Side }`
- [ ] Объявить `Side`: `Attacker`, `Defender`

### 5.4 Механика боя

- [ ] Функция `BattleState::current_stack()` — чей ход
- [ ] Функция `BattleState::attack(target_id) -> BattleEvent`:
  - вычислить урон: `damage = count * unit_base_damage`
  - вычислить убитых: `killed = damage / target.unit_type.hp`
  - уменьшить `count` цели, обновить `hp_remaining`
  - если цель мертва — убрать из очереди
- [ ] Функция `BattleState::next_turn()` — передать ход следующему стеку
- [ ] Функция `BattleState::is_over() -> Option<Side>` — проверить победителя

### 5.5 Отрисовка боя

- [ ] Отрисовать два ряда стеков (атакующие слева, защитники справа)
- [ ] Показать для каждого стека: название, количество, HP
- [ ] Подсветить текущий активный стек
- [ ] Показать кнопку `Атаковать` (выбрать цель кликом)

### 5.6 Простой AI защитника

- [ ] Когда ход переходит к нейтральному стеку — автоматически атаковать первый живой стек атакующего

### 5.7 Завершение боя

- [ ] При победе атакующего:
  - убрать `NeutralArmy` с карты
  - обновить армию героя (учесть потери)
  - вернуться на карту (`GameScreen::Adventure`)
  - показать кратный лог или баннер `"Победа!"`
- [ ] При поражении атакующего:
  - показать `"Поражение"`
  - (пока) перезапустить игру или заморозить состояние

**Критерий готовности:** игрок побеждает нейтральный отряд и продолжает игру на карте.

---

## Этап 6 — Вертикальный срез

**Цель:** собрать играбельную мини-игру от старта до финала.

### 6.1 Дизайн стартовой карты

- [ ] Карта 20×15 тайлов
- [ ] Расстановка объектов:
  - стартовый город (позиция игрока)
  - 4–5 кучек золота
  - 3 нейтральных отряда разной силы
  - несколько препятствий

### 6.2 Стартовое состояние

- [ ] Герой со стартовой армией (1 стек слабых существ)
- [ ] Начальный запас золота (например, 500)
- [ ] Начальный доход города

### 6.3 Условие победы

- [ ] Объявить `VictoryCondition`: победить всех нейтральных на карте
- [ ] Проверять после каждого боя: `GameState::check_victory() -> Option<GameResult>`
- [ ] Возвращать `GameResult::Victory` или `GameResult::Defeat`

### 6.4 Экран победы / поражения

- [ ] Создать `GameScreen::GameOver(GameResult)`
- [ ] Отрисовать простой экран с текстом `"Победа!"` / `"Поражение"`
- [ ] Кнопка `"Играть снова"` — перезапустить `GameState`

### 6.5 Полировка петли

- [ ] Убедиться что переходы между экранами (карта → город → бой → карта) работают без паники
- [ ] Убедиться что `EndTurn` корректно обновляет все системы
- [ ] Проверить что нельзя двигаться без MP, нанимать без золота и т.д.
- [ ] Добавить базовый лог событий (в консоль или в UI-панель)

**Критерий готовности:** в игру можно сыграть от начала до конца за 5–10 минут.

---

## Этап 7 — Расширение (после вертикального среза)

Добавлять только после того, как Этап 6 полностью готов.

### 7.1 Контент

- [ ] Больше типов существ (2–3 на каждый уровень)
- [ ] Несколько фракций с разными войсками
- [ ] Артефакты для героя

### 7.2 Механики героя

- [ ] Опыт и уровни
- [ ] Навыки (2–3 ветки: атака, защита, магия)
- [ ] Заклинания и мана

### 7.3 Карта мира

- [ ] Fog of war
- [ ] Шахты ресурсов (дают доход каждый день)
- [ ] Дороги (бонус к MP)
- [ ] Несколько типов ресурсов (дерево, руда и т.д.)

### 7.4 Города

- [ ] Дерево зданий
- [ ] Рынок
- [ ] Гарнизон (защита города без героя)
- [ ] Магическая гильдия

### 7.5 Боевая система

- [ ] Инициатива и скорость существ
- [ ] Контратака
- [ ] Мораль и удача
- [ ] Дальний бой (лучники)
- [ ] Магия в бою
- [ ] Препятствия на поле боя
- [ ] Автобой

### 7.6 AI

- [ ] AI-противник: простой обход карты, захват ресурсов
- [ ] Оценка силы армий
- [ ] AI в бою: выбор оптимальной цели

### 7.7 Технические улучшения

- [ ] Сохранение и загрузка (`serde` + `ron`)
- [ ] Генератор карт (процедурный)
- [ ] Звук и музыка (`bevy_audio`)
- [ ] Анимации (`bevy_sprite`)
- [ ] Локализация (RU/EN)
- [ ] Hotseat для двух игроков

---

## Definition of Done

Фича считается готовой, если:

1. Она работает в игре.
2. Для чистой логики есть хотя бы один unit-тест.
3. Она не ломает существующую игровую петлю.
4. Код прошёл `cargo fmt` и `cargo clippy` без предупреждений.
5. Нет необработанных `unwrap()`, `panic!()`, `todo!()` в пользовательском пути.
